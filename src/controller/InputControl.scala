package controller

import javafx.event.{ActionEvent, EventHandler}
import model.gameRules

class InputControl {

}

class cardSubmitted(text: String) extends EventHandler[ActionEvent]{
  override def handle(event: ActionEvent): Unit = {

  }
}

class cardSelected(text: String) extends EventHandler[ActionEvent]{
  override def handle(event: ActionEvent): Unit = {

  }
}
